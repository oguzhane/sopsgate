info:
  name: Delete Secret
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/secrets/{{namespace}}/keys/{{key}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
