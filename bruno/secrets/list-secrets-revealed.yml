info:
  name: List Secrets (Revealed)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/secrets/{{namespace}}?reveal=true"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
