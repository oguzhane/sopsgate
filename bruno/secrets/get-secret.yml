info:
  name: Get Secret
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/secrets/{{namespace}}/keys/{{key}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
