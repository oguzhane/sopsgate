info:
  name: Get Secret at Version
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/secrets/{{namespace}}/keys/{{key}}/versions/{{version}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
