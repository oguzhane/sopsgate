info:
  name: List Versions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/secrets/{{namespace}}/keys/{{key}}/versions"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
